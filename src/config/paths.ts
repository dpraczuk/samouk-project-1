export const paths = {
  home: '/',
  auth: {
    login: '/auth/login',
    register: '/auth/register',
  },
  app: {
    root: '/app',
    dashboard: '/app',
    profile: '/app/profile',
  },
} as const;
